{% comment %} faq.liquid — Phase 1.3 {% endcomment %}
<div class="section-container section-padding">
  <h1 class="font-heading text-3xl font-bold text-text mb-8">{{ page.title }}</h1>
  <div class="text-text-light">{{ page.content }}</div>
</div>

{% schema %}
{
  "name": "FAQ",
  "tag": "section",
  "settings": []
}
{% endschema %}
