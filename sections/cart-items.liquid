{% comment %} cart-items.liquid — Phase 1.2 {% endcomment %}
<div class="section-container section-padding">
  <h1 class="font-heading text-3xl font-bold text-text mb-8">{{ 'cart.general.title' | t }}</h1>
  <p class="text-text-light">{{ 'cart.general.empty' | t }}</p>
</div>

{% schema %}
{
  "name": "Panier",
  "tag": "section",
  "settings": []
}
{% endschema %}
